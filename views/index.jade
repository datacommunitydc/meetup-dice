extends layout

block content
  - var i = -3;
    while i <= res.length
      - i += 3;
      .row
        each grp in res.slice(i, i+3)
          .col-md-4.grp
            a(href='/#{grp.urlname}')
              p=grp.name
              img(src='#{grp.group_photo.thumb_link}')
